[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "toolstart"
version = "0.2.0"
description = "GPG-encrypted secret injector with interactive profile picker"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "NamiLink" }
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Security :: Cryptography",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]


requires-python = ">=3.10"
dependencies = [
    "pyyaml",
]

[project.urls]
Repository = "https://github.com/namilinklabs/toolstart"
Documentation = "https://github.com/namilinklabs/toolstart"


[project.scripts]
toolstart = "toolstart:main"

[tool.hatch.build.targets.wheel]
include = ["toolstart.py"]

[tool.uv]
publish-url = "https://test.pypi.org/legacy/"
